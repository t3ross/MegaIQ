// import store from 'src/store';

// const isAuthenticatedGuard = async (tdo, from, next) => {
//   const { ok } = await store.dispatch('auth/checkAuthentication');

//   if (ok) next();
//   else next({ name: 'login' });
// };

// export default isAuthenticatedGuard;
